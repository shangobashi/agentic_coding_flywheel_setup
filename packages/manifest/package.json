{
  "name": "@acfs/manifest",
  "version": "0.6.0",
  "private": true,
  "description": "Parser and utilities for ACFS manifest files",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf dist",
    "generate": "bun run src/generate.ts",
    "generate:dry": "bun run src/generate.ts --dry-run --verbose",
    "generate:validate": "bun run src/generate.ts --validate",
    "generate:diff": "bun run src/generate.ts --diff"
  },
  "dependencies": {
    "yaml": "^2.8.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.19.11",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  }
}
